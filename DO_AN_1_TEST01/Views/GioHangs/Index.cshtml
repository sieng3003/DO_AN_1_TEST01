@model IEnumerable<DO_AN_1_TEST01.Models.GioHang>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TongTien)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TongTien)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GioHangId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GioHangId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GioHangId })
        </td>
    </tr>
}

</table>
